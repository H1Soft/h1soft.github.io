---
import Icon from './Icon.astro';
import AppIcon from './AppIcon.astro';
import StoreLinks from './StoreLinks.astro';
import { cities, faqs, asset, type Lang } from '../data/site';
const { lang = 'en' } = Astro.props as { lang: Lang };
const c = (en: string, ko: string) => (lang === 'ko' ? ko : en);
const motif = [
  '0000000000',
  '0000000000',
  '0000110000',
  '0011110222',
  '0011111222',
  '1111111222',
  '1111111222',
  '1111111222',
  '0000000000',
  '1111222333',
];
const comfort = [
  [
    'hand',
    c('One hand, at ease', '한 손으로 편하게'),
    c('Move the board and controls to your side.', '보드와 도구를 편한 쪽에 두세요.'),
  ],
  [
    'moon',
    c('Night flight', '야간 비행'),
    c('A quieter palette for the later hours.', '늦은 시간에는 차분한 색으로.'),
  ],
  [
    'offline',
    c('Ready for airplane mode', '비행기 모드에서도'),
    c('Puzzles, hints and your album work offline.', '퍼즐, 힌트, 앨범을 오프라인으로.'),
  ],
  [
    'globe',
    c('Two familiar languages', '익숙한 두 언어'),
    c('English and Korean, throughout the app.', '앱 전체를 한국어와 영어로.'),
  ],
  [
    'save',
    c('Right where you left off', '멈췄던 그 칸에서'),
    c('Your progress saves as you go.', '플레이하는 동안 진행을 자동 저장해요.'),
  ],
  [
    'tune',
    c('Your flight, your controls', '나에게 맞는 비행'),
    c(
      'Autopilot feedback, or manual flight without error rings.',
      '자동 항법 또는 수동 비행을 선택하세요.',
    ),
  ],
  [
    'eye',
    c('More than colour', '색 너머의 단서'),
    c('Numbers and patterns help tell inks apart.', '번호와 패턴으로 잉크를 구분해요.'),
  ],
  [
    'book',
    c('Clues that can be read aloud', '소리로 읽는 단서'),
    c('Cells describe their row, column and state.', '칸의 행, 열, 상태를 읽어줍니다.'),
  ],
];
---

<section class="hero" id="home" aria-labelledby="hero-heading">
  <div class="hero-art" aria-hidden="true">
    <img
      src={asset('hero-icon-art.webp?v=20260920-full-bleed')}
      width="1024"
      height="1024"
      alt=""
      fetchpriority="high"
    />
  </div>
  <div class="container hero-content">
    <div class="hero-copy">
      <h1 id="hero-heading">
        {c('Every puzzle is a place you’ve been.', '퍼즐 하나가 다녀왔던 곳이 됩니다.')}
      </h1>
      <p class="hero-description">
        {c(
          'Board 192 flights across twelve cities. Read the clues, fill the grid, and the view outside your window develops into a photo for your album.',
          '열두 도시, 192편의 항공편에 탑승합니다. 숫자를 읽어 격자를 채우면 창밖 풍경이 앨범에 남을 사진으로 현상됩니다.',
        )}
      </p>
      <StoreLinks lang={lang} position="hero" />
      <p class="release-note">
        {c('Coming soon for iOS and Android.', 'iOS와 Android 앱으로 곧 만나요.')}
      </p>
      <a class="text-link" href="#route">
        {c('Find your next window view', '다음 창밖 풍경 둘러보기')}{' '}
        <span aria-hidden="true">↓</span>
      </a>
    </div>
  </div>
</section>
<div class="sticky-sentinel" aria-hidden="true"></div>
<section class="trust-strip" aria-label={c('App information', '앱 정보')}>
  <div class="container trust-inner">
    <span class="trust-brand">
      <AppIcon size={34} />
      {c('Made for a little escape.', '잠깐의 여행을 위해.')}
    </span>
    <span class="airdata">ANDROID 8.0+ · iOS 15+</span>
    <span class="airdata">EN / KO</span>
    <span class="airdata">{c('NO ACCOUNT NEEDED', '회원가입 없이')}</span>
  </div>
</section>
<section class="section route-section" id="route" data-city="icn" aria-labelledby="route-heading">
  <div class="container">
    <div class="section-heading">
      <p class="airdata accent">YOUR ROUTE · 12 CITIES</p>
      <h2 id="route-heading">
        {c('A different city. A different kind of colour.', '도시가 바뀌면, 풍경의 색도 바뀝니다.')}
      </h2>
      <p>
        {c(
          'Twelve regions, sixteen flights each. Every destination has its own palette, and a place in your album. Pick your next window view.',
          '열두 구역, 구역마다 열여섯 편. 도시는 저마다의 팔레트로 앨범에 남습니다. 다음 창밖 풍경을 골라보세요.',
        )}
      </p>
    </div>
    <div class="route-line" aria-hidden="true">
      <svg viewBox="0 0 1100 90" preserveAspectRatio="none">
        <path
          class="route-path"
          d="M10 65 C180 15 320 105 520 50 C720 0 870 5 1090 60"
          fill="none"
          stroke="currentColor"
          stroke-width="1.5"
          pathLength="1"
        />
      </svg>
      <span>
        <Icon name="plane" size={26} />
      </span>
    </div>
    <div
      class="city-tabs"
      role="tablist"
      aria-label={c('Choose a destination', '여행할 도시 선택')}
    >
      {cities.map((city, i) => (
        <button
          id={`tab-${city.code.toLowerCase()}`}
          role="tab"
          aria-selected={i === 0 ? 'true' : 'false'}
          aria-controls={`city-${city.code.toLowerCase()}`}
          tabindex={i === 0 ? 0 : -1}
          data-code={city.code.toLowerCase()}
        >
          <span class="city-dot" style={`--dot:${city.colors[0]}`}></span>
          <strong>{city.code}</strong>
          <span>{city[lang]}</span>
        </button>
      ))}
    </div>
    <div class="route-panels">
      {cities.map((city, i) => (
        <div
          id={`city-${city.code.toLowerCase()}`}
          class="city-panel"
          role="tabpanel"
          aria-labelledby={`tab-${city.code.toLowerCase()}`}
          hidden={i !== 0}
          tabindex="0"
        >
          <div class="city-photo">
            <img
              src={i === 0 ? asset(`city-${city.code.toLowerCase()}.avif`) : undefined}
              data-src={asset(`city-${city.code.toLowerCase()}.avif`)}
              width="1600"
              height="900"
              loading="lazy"
              decoding="async"
              alt={lang === 'ko' ? `${city.ko}의 여행 일러스트` : `Illustrated view of ${city.en}`}
            />
            <div class="photo-ticket">
              <span class="airdata">{String(i + 1).padStart(2, '0')} / 12</span>
              <strong>{city.code}</strong>
              <span>{city[lang]}</span>
              <span class="airdata">16 FLIGHTS</span>
            </div>
          </div>
          <div class="city-story">
            <span class="airdata">WINDOW SEAT · {city.code}</span>
            <h3>{city[lang === 'ko' ? 'themeKo' : 'themeEn']}</h3>
            <p>
              {c(
                'One view to discover. Sixteen little reasons to stay.',
                '발견하고 싶은 풍경 하나. 조금 더 머무르고 싶은 열여섯 가지 이유.',
              )}
            </p>
            <div class="swatches" aria-label={c('City colour palette', '도시 팔레트')}>
              {city.colors.map((color) => (
                <span style={`background:${color}`}>
                  <span class="sr-only">{color}</span>
                </span>
              ))}
            </div>
            <div
              class="city-puzzle"
              aria-hidden="true"
              style={`--c1:${city.colors[0]};--c2:${city.colors[1]};--c3:${city.colors[2]}`}
            >
              {motif
                .join('')
                .split('')
                .map((v) => (
                  <i style={`background:${v === '0' ? 'transparent' : `var(--c${v})`}`} />
                ))}
            </div>
            <span class="caption">{c('A palette preview', '도시 색 미리보기')}</span>
          </div>
        </div>
      ))}
    </div>
    <p class="route-foot caption">
      {c('Choose a city to explore its colours.', '도시를 골라 색을 둘러보세요.')}
    </p>
    <noscript>
      <p>
        {c(
          'Enable JavaScript to switch cities. Your first view is Seoul.',
          '도시를 바꾸려면 JavaScript를 켜 주세요. 첫 풍경은 서울입니다.',
        )}
      </p>
    </noscript>
  </div>
</section>
<section class="section develop-section" id="develop">
  <div class="container develop-layout">
    <div class="develop-art">
      <div class="development-frame">
        <div class="develop-image" data-develop style="--develop:50%">
          <img
            src={asset('city-hkg.avif')}
            alt={c('Hong Kong’s harbour coming into colour', '색이 입혀지는 홍콩의 항구')}
            width="1600"
            height="900"
            loading="lazy"
          />
          <div class="develop-colour-pixels" aria-hidden="true">
            {motif
              .join('')
              .split('')
              .map((v) => (
                <i style={`background:${v === '0' ? '#f2ede2' : 'transparent'}`} />
              ))}
          </div>
          <div class="develop-mono" aria-hidden="true">
            <div class="develop-ink-pixels">
              {motif
                .join('')
                .split('')
                .map((v) => (
                  <i style={`background:${v === '0' ? '#f2ede2' : '#12222e'}`} />
                ))}
            </div>
          </div>
          <span class="develop-line" aria-hidden="true"></span>
          <span class="arrival-stamp" aria-hidden="true">
            ARRIVED<small>HKG · WINDOW SEAT</small>
          </span>
        </div>
        <div class="develop-caption">
          <strong>{c('Harbour lights', '항구의 불빛')}</strong>
          <span class="airdata">HONG KONG · HKG</span>
        </div>
      </div>
      <label class="range-label" for="develop-range">
        <span>{c('Develop the view', '풍경에 색 입히기')}</span>
        <output id="develop-value" for="develop-range">
          50%
        </output>
      </label>
      <input
        id="develop-range"
        type="range"
        min="0"
        max="100"
        value="50"
        aria-label={c('Develop amount', '채색 정도')}
      />
      <p class="caption">
        {c(
          'Slide to see the colour come through.',
          '슬라이더를 움직여 색이 스며드는 모습을 보세요.',
        )}
      </p>
    </div>
    <div class="develop-copy">
      <p class="airdata accent">FROM LOGIC TO A MEMORY</p>
      <h2>
        {c('Fill the last cell. Let the colour in.', '마지막 칸을 채우면, 도시의 색이 스며듭니다.')}
      </h2>
      <p>
        {c(
          'Follow the clues, one square at a time. Finish the puzzle and it becomes a little photograph in the city’s colours, ready for your album.',
          '숫자를 따라 한 칸씩 채워보세요. 완성한 퍼즐은 그 도시의 색을 담은 작은 사진이 되어 앨범에 남습니다.',
        )}
      </p>
      <div class="arrival-steps">
        <div>
          <span class="small-stamp">
            ARRIVED
            <br />
            <small>HKG</small>
          </span>
          <span>{c('Make your landing', '도착을 기록하고')}</span>
        </div>
        <Icon name="arrow" />
        <div>
          <img src={asset('prop_polaroids.webp')} width="94" height="72" alt="" loading="lazy" />
          <span>{c('Keep the memory', '추억을 모으세요')}</span>
        </div>
      </div>
    </div>
  </div>
</section>
<section class="section learn-section" id="learn">
  <div class="container learn-layout">
    <div>
      <p class="airdata accent">SMART HINT · THREE SMALL STEPS</p>
      <h2>{c('Learn to see the next move.', '정답보다, 다음 수를 읽는 법.')}</h2>
      <p>
        {c(
          'A good hint does more than fill a square. It shows where to look, explains the logic, then lets you see what is certain. Eleven handbook lessons help along the way.',
          '좋은 힌트는 칸을 대신 채우는 데서 멈추지 않습니다. 볼 곳을 짚고, 이유를 설명하고, 확실한 칸을 보여줍니다. 열한 가지 운항 교본 수업도 함께합니다.',
        )}
      </p>
    </div>
    <div class="hint-card" data-hints>
      <div class="hint-tabs" role="tablist" aria-label={c('Hint steps', '힌트 단계')}>
        {[c('Look', '어디를 볼까요'), c('Understand', '왜 그럴까요'), c('Confirm', '확인하기')].map(
          (text, i) => (
            <button
              role="tab"
              aria-selected={i === 0 ? 'true' : 'false'}
              id={`hint-tab-${i}`}
              aria-controls={`hint-panel-${i}`}
              tabindex={i === 0 ? 0 : -1}
              data-step={i}
            >
              {i + 1}
              <span>{text}</span>
            </button>
          ),
        )}
      </div>
      {[
        c(
          'Look at this row. Three connected cells must fit in five spaces.',
          '이 행을 보세요. 다섯 칸에 연속된 세 칸이 들어가야 합니다.',
        ),
        c(
          'Push the run to either end. The middle cell is filled in both cases.',
          '세 칸을 왼쪽과 오른쪽 끝에 놓아보면, 가운데 칸은 언제나 겹칩니다.',
        ),
        c(
          'That middle cell is certain. You found it by logic, without guessing.',
          '가운데 칸은 확실히 칠할 수 있습니다. 추측하지 않고 논리로 찾았습니다.',
        ),
      ].map((text, i) => (
        <div
          class="hint-panel"
          id={`hint-panel-${i}`}
          role="tabpanel"
          aria-labelledby={`hint-tab-${i}`}
          hidden={i !== 0}
        >
          <span class="airdata">{i === 1 ? 'OVERLAP · T2' : c('ROW CLUE', '행 힌트')}</span>
          <div class="hint-row">
            <strong>3</strong>
            {Array.from({ length: 5 }, (_, j) => (
              <span class:list={{ certain: i === 2 && j === 2, possible: i === 1 }}>
                {i === 2 && j === 2 ? <Icon name="check" /> : ''}
              </span>
            ))}
          </div>
          {i === 1 && (
            <div class="overlap-bars" aria-hidden="true">
              <i></i>
              <i></i>
            </div>
          )}
          <p>{text}</p>
        </div>
      ))}
    </div>
  </div>
</section>
<section class="section collect-section" id="collect" aria-labelledby="collect-heading">
  <div class="container">
    <div class="memory-heading">
      <h2 id="collect-heading">{c('A journey worth keeping.', '여정이 앨범으로 남습니다.')}</h2>
      <p>
        {c(
          'A sunset in Seoul. Harbour lights in Hong Kong. A night beneath the northern lights. Keep the places you discover, one square at a time.',
          '서울의 노을, 홍콩의 불빛, 아이슬란드의 밤. 한 칸씩 완성한 풍경을 사진으로 간직하세요.',
        )}
      </p>
    </div>
    <div class="memory-album">
      <div class="memory-album-label airdata">
        <span>NONOGRAM TRIP</span>
        <span>{c('YOUR TRAVEL ALBUM', '나의 여행 앨범')}</span>
      </div>
      <div class="memory-spread">
        {[
          {
            code: 'ICN',
            place: c('Seoul', '서울'),
            title: c('The light we leave with', '여행이 시작되는 노을'),
            alt: c(
              'Warm sunset over the Han River in Seoul',
              '한강과 서울의 스카이라인 위로 지는 노을',
            ),
          },
          {
            code: 'HKG',
            place: c('Hong Kong', '홍콩'),
            title: c('A harbour full of stories', '불빛으로 기억할 도시'),
            alt: c(
              'Hong Kong harbour and its colourful skyline',
              '홍콩 항구와 빛나는 도시의 스카이라인',
            ),
          },
          {
            code: 'KEF',
            place: c('Iceland', '아이슬란드'),
            title: c('A little closer to the sky', '하늘과 조금 더 가까이'),
            alt: c(
              'Northern lights over Iceland’s mountains',
              '아이슬란드의 산 위로 펼쳐지는 오로라',
            ),
          },
        ].map((memory, i) => (
          <figure class:list={['memory-photo', { 'memory-photo-main': i === 1 }]}>
            <img
              src={asset(`city-${memory.code.toLowerCase()}.avif`)}
              width="1600"
              height="900"
              loading="lazy"
              decoding="async"
              alt={memory.alt}
            />
            <figcaption>
              <span class="memory-destination">
                <strong>{memory.place}</strong>
                <span class="airdata">{memory.code}</span>
              </span>
              <span class="memory-photo-title">{memory.title}</span>
            </figcaption>
          </figure>
        ))}
      </div>
      <div class="memory-album-bottom">
        <p>{c('Some places stay with you.', '다녀온 풍경은 오래 남으니까.')}</p>
        <span class="airdata">
          {c('ALBUM PREVIEW · 12 DESTINATIONS', '앨범 미리보기 · 12개의 여행지')}
        </span>
      </div>
    </div>
    <div class="memory-keepsakes">
      <div class="memory-keepsake">
        <div class="memory-tag" aria-hidden="true">
          <span>ICN</span>
          <i></i>
        </div>
        <div>
          <h3>{c('Little souvenirs. Your own story.', '작은 기념품까지, 나의 여행답게.')}</h3>
          <p>
            {c(
              'Discover 36 baggage tags along the way. Each one keeps a little piece of the journey.',
              '여정에서 만나는 서른여섯 가지 수하물 태그. 작은 기념품에도 여행의 기억이 담깁니다.',
            )}
          </p>
        </div>
      </div>
      <div class="memory-keepsake">
        <div class="memory-stamp" aria-hidden="true">
          <span>NONOGRAM TRIP</span>
          <strong>ICN</strong>
          <span>ARRIVED</span>
        </div>
        <div>
          <h3>{c('A stamp for every destination.', '도시를 완주하면, 여권에 한 장.')}</h3>
          <p>
            {c(
              'Finish a city and collect its passport stamp. Watch your travels fill the pages.',
              '도시를 완주할 때마다 여권에 스탬프가 남아요. 다음 페이지는 어떤 도시가 채워질까요?',
            )}
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
<section class="section free-section" id="free">
  <div class="container free-layout">
    <div>
      <p class="airdata">YOUR TICKET INCLUDES EVERYTHING</p>
      <h2>{c('Every feature is free.', '모든 기능이 무료입니다.')}</h2>
      <p>
        {c(
          'No paywall. No second tier. Just you, a little logic, and somewhere new to go.',
          '유료 기능 등급 없이. 나와 작은 논리, 그리고 새로운 여행지만 있으면 됩니다.',
        )}
      </p>
      <StoreLinks lang={lang} position="free_section" />
    </div>
    <ul class="included-list">
      {[
        c('All twelve cities', '열두 도시 모두'),
        c('Every boarding class', '모든 탑승 클래스'),
        c('Unlimited hints', '무제한 힌트'),
        c('Every photo, tag and stamp', '모든 사진 · 태그 · 스탬프'),
        c('Offline play', '오프라인 플레이'),
        c('No account needed', '회원가입 없이'),
        c('No energy or heart limits', '에너지 · 하트 제한 없이'),
        c('No time pressure', '시간에 쫓기지 않고'),
      ].map((text) => (
        <li>
          <Icon name="check" />
          {text}
        </li>
      ))}
    </ul>
  </div>
</section>
<section class="section comfort-section">
  <div class="container">
    <div class="section-heading">
      <p class="airdata accent">SETTLE INTO YOUR SEAT</p>
      <h2>{c('Built for a comfortable flight.', '긴 비행도 편안하도록.')}</h2>
    </div>
    <div class="comfort-grid">
      {comfort.map(([icon, title, body]) => (
        <div>
          <Icon name={icon} size={25} />
          <h3>{title}</h3>
          <p>{body}</p>
        </div>
      ))}
    </div>
  </div>
</section>
<section class="section video-section" id="film">
  <div class="container video-layout">
    <div>
      <p class="airdata accent">A MOMENT ON BOARD</p>
      <h2>{c('From your first flight to your first photo.', '첫 비행부터 첫 사진까지.')}</h2>
      <p>
        {c(
          'A short look inside Nonogram Trip. Choose a flight, follow the clues, and bring a new memory home.',
          '노노그램 트립 안으로 잠깐 들어가 볼까요. 항공편을 고르고, 숫자를 따라 풀고, 새로운 추억을 가져오세요.',
        )}
      </p>
      <span class="airdata">{c('APP PREVIEW · 35 SECONDS', '앱 미리보기 · 35초')}</span>
    </div>
    <div class="video-frame">
      <video
        controls
        preload="none"
        playsinline
        poster={asset('video-poster.webp')}
        width="720"
        height="1600"
        aria-label={c('Nonogram Trip app walkthrough', '노노그램 트립 앱 둘러보기')}
      >
        <source src="/nonogram/media/nonogram-trip.mp4" type="video/mp4" />
        <source src="/nonogram/media/nonogram-trip.webm" type='video/webm; codecs="vp9"' />
        <track
          default
          kind="captions"
          src={`/nonogram/media/captions-${lang}.vtt`}
          srclang={lang}
          label={lang === 'ko' ? '한국어' : 'English'}
        />
        <track
          kind="captions"
          src={`/nonogram/media/captions-${lang === 'ko' ? 'en' : 'ko'}.vtt`}
          srclang={lang === 'ko' ? 'en' : 'ko'}
          label={lang === 'ko' ? 'English' : '한국어'}
        />
        <p>
          {c('Your browser cannot play this video.', '브라우저에서 영상을 재생할 수 없습니다.')}
        </p>
      </video>
    </div>
  </div>
</section>
<section class="section faq-section" id="faq">
  <div class="container faq-layout">
    <div>
      <p class="airdata accent">BEFORE YOU BOARD</p>
      <h2>{c('A few things you might be wondering.', '탑승 전에 궁금한 것들.')}</h2>
      <p>
        {c(
          'Anything else? We’re a short email away.',
          '더 궁금한 점이 있다면 이메일로 알려주세요.',
        )}
      </p>
      <a class="text-link" href={lang === 'ko' ? '/nonogram/ko/support/' : '/nonogram/support/'}>
        {c('Visit Support', '지원 페이지')}
        <Icon name="arrow" size={18} />
      </a>
    </div>
    <div class="faq-list">
      {faqs[lang].map((faq, i) => (
        <details>
          <summary>
            {faq.q}
            <span aria-hidden="true">+</span>
          </summary>
          <div>
            <p>{faq.a}</p>
            {i === 1 && (
              <a class="text-link" href="#route">
                {c('Explore the destinations', '여행지 둘러보기')}
                <Icon name="arrow" size={16} />
              </a>
            )}
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
<section class="final-cta" id="download">
  <div class="container">
    <p class="airdata">BOARDING SOON · WINDOW SEAT RESERVED</p>
    <h2>{c('Your first window seat is waiting.', '첫 창가 좌석이 기다리고 있습니다.')}</h2>
    <StoreLinks lang={lang} position="footer_cta" />
    <a href="#route" class="final-explore">
      {c('Explore the journey', '여정 둘러보기')}
      <Icon name="arrow" />
    </a>
    <svg class="final-arc" viewBox="0 0 1100 220" fill="none" aria-hidden="true">
      <path
        d="M-40 210C280-120 770-70 1160 200"
        stroke="currentColor"
        stroke-width="1"
        stroke-dasharray="6 9"
      />
    </svg>
  </div>
</section>
<div class="mobile-sticky" hidden>
  <span>
    <strong>Nonogram Trip</strong>
    <small>{c('Coming soon on mobile', '모바일 앱 출시 예정')}</small>
  </span>
  <a class="button" href="#route">
    {c('Explore cities', '여행지 보기')}
    <Icon name="arrow" size={16} />
  </a>
</div>
<script>
  import '../scripts/landing';
</script>
